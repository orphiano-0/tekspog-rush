import 'package:flutter/material.dart';

class BettingDialog extends StatelessWidget {
  const BettingDialog({super.key});

  @override
  Widget build(BuildContext context) {
    return Dialog();
  }
}
